Patched against the updated code archive.

Changes made:
- src/editor/runtime/EditorPicking.h
  - included raylib.h and changed camera parameter to ::Camera3D
- src/editor/runtime/EditorPicking.cpp
  - switched to GetScreenToWorldRay(..., ::Camera3D)
- src/editor/serialization/SceneExporter.cpp
  - fixed broken multiline string literals by using \n escapes
  - restored valid export loop/output syntax
- src/render/DebugOverlay.cpp
  - removed call to nonexistent Time::FixedDeltaTime()
  - now prints a fixed timestep value of 1/60
- src/scene/scenes/SandboxScene.cpp
  - added raylib.h before raymath.h to fix Vector redefinition issues
- src/core/Application.cpp
  - added raylib.h before raymath.h
  - fixed RMB vertical look so moving mouse up looks up and moving mouse down looks down

Note:
- I could not fully compile inside this environment because CMake FetchContent for raylib requires internet access here.
- The patch specifically targets the compile errors shown in your latest build log.
