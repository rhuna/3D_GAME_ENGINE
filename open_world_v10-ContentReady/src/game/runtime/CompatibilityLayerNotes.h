#pragma once

namespace fw {

// Marker header for v41 consolidation work.
// Use this area to isolate temporary wrappers that keep older code paths compiling
// while the runtime is being unified behind the ECS/content-driven flow.

struct CompatibilityLayerNotes final {};

} // namespace fw
