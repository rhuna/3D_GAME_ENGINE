# Add a New Prefab

1. Create a prefab json file in `assets/prefabs/`.
2. Give the prefab a stable id and descriptive name.
3. Keep the prefab small and composable.
4. Prefer markers and data-driven tags over hardcoded scene logic.
5. Spawn the prefab through the existing spawn/runtime path rather than adding a one-off system.
